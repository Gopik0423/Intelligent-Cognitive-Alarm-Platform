# FastAPI Framework
fastapi==0.138.2
starlette==1.3.1
uvicorn==0.49.0

# Database
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12

# Authentication & Security
python-jose==3.5.0
passlib==1.7.4
bcrypt==5.0.0
python-multipart==0.0.32

# Data Validation
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
annotated-doc==0.0.4
typing-inspection==0.4.2
typing_extensions==4.15.0
# Networking
anyio==4.14.1
h11==0.16.0
idna==3.18

# Cryptography
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.3
six==1.17.0

# CLI
click==8.4.2

# Optional push notification provider (set FIREBASE_SERVICE_ACCOUNT_FILE to enable)
firebase-admin==7.1.0

# AI puzzle generation (ai_generator.py) -- previously used but not declared here,
# which broke `pip install -r requirements.txt` on a fresh machine
google-generativeai==0.8.6
python-dotenv==1.2.2

# Data analysis (services/analytics.py, services/behavioral_analytics.py) --
# same issue: imported but never declared. Pinned to 2.2.2 specifically:
# newer 3.x releases have been flagged by some Windows endpoint-security
# software (Device Guard / Application Control) as blocking a compiled
# pandas DLL on install, which 2.2.2 does not trigger.
pandas==2.2.2

# Test suite (tests/) -- required to run pytest at all, was undeclared
pytest==8.3.4
